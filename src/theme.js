import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    jis: {
      red: '#FF4B00',
      orange: '#F6AA00',
      yellow: 'F2E700',
      green: '#00B06B',
      blue: '#1971FF',
      redPurple: '#990099',
      white: '#FFFFFF',
      black: '#000000'
    }
  }
})

export default theme
